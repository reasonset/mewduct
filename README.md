# ![](doc/img/mewduct-logo.png) Mewduct

Video streaming CMS without a server-side application

# Install

1. Copy under `webroot/` to your web serving root (e.g. `/srv/http/`)
2. Edit `config.js`.
3. 

# Update

```bash
rsync -rv --exclude config.js webroot/ /path/to/webroot/
```

