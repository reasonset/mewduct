var MEWDUCT_CONFIG = {
  // plyr script path.
  // This value becomes the src attribute of the script element. It is expected to be a path to plyr.js. If not specified, it will be loaded from plyr's CDN.
  //plyr_script_path: "",

  // plyr CSS path.
  // This value becomes the href value of the link element that loads the CSS. If not specified, it will be loaded from Plyr's CDN.
  //plyr_css_path: ""
}